name: Samples OCaml (with recursive types)

on:
  push:
    paths:
      - 'samples/client/petstore/ocaml-recursive/**'
      - 'samples/client/petstore/ocaml-recursive-fake-petstore/**'
      - 'samples/client/petstore/ocaml-recursive-oneOf-primitive/**'
      - 'samples/client/petstore/ocaml-recursive-additional-properties/**'
  pull_request:
    paths:
      - 'samples/client/petstore/ocaml-recursive/**'
      - 'samples/client/petstore/ocaml-recursive-fake-petstore/**'
      - 'samples/client/petstore/ocaml-recursive-oneOf-primitive/**'
      - 'samples/client/petstore/ocaml-recursive-additional-properties/**'

jobs:
  build:
    name: Build OCaml
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        sample:
          - 'samples/client/petstore/ocaml-recursive/'
          - 'samples/client/petstore/ocaml-recursive-fake-petstore/'
          - 'samples/client/petstore/ocaml-recursive-oneOf-primitive/'
          - 'samples/client/petstore/ocaml-recursive-additional-properties/'
    steps:
      - uses: actions/checkout@v5
      - name: Set-up OCaml
        uses: ocaml/setup-ocaml@v3
        with:
          ocaml-compiler: 5
      - name: Install
        run: opam install . --deps-only --with-test
        working-directory: ${{ matrix.sample }}
      - name: Build
        run: opam exec -- dune build
        working-directory: ${{ matrix.sample }}
